import { LoginRequest, RegisterRequest } from '@/types/auth';
// import { GeneralResponse } from '@/types/misc';

import { $http } from './index';

export default {
  /* login: (loginRequestArgs: LoginRequest): Promise<GeneralResponse> => {
    return $http.POST('/id/login', '', loginRequestArgs);
  },
  register: (registerRequestArgs: RegisterRequest): Promise<GeneralResponse> => {
    return $http.POST('/id/login', registerRequestArgs);
  },*/
};
